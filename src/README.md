# src

This directory will contain the project source code.

Add source files and subdirectories here as the project grows.
Refer to `docs/architecture.md` for the intended structure and `docs/coding-standards.md` for style conventions.
